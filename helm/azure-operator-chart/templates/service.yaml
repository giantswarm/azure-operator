apiVersion: v1
kind: Service
metadata:
  name: azure-operator
  namespace: {{ .Values.namespace }}
  labels:
    app: azure-operator
    version: thiccc
  annotations:
    prometheus.io/scrape: "true"
spec:
  type: NodePort
  ports:
  - port: 8000
  selector:
    app: azure-operator
    version: thiccc
