version: 1

test:
  env:
    - "AZURE_AZS=1"
    - "AZURE_CALICO_SUBNET_CIDR=${AZURE_CALICO_SUBNET_CIDR}"
    - "AZURE_CIDR=${AZURE_CIDR}"
    - "AZURE_CLIENTID=${AZURE_CLIENTID}"
    - "AZURE_CLIENTSECRET=${AZURE_CLIENTSECRET}"
    - "AZURE_GUEST_CLIENTID=${AZURE_GUEST_CLIENTID}"
    - "AZURE_GUEST_CLIENTSECRET=${AZURE_GUEST_CLIENTSECRET}"
    - "AZURE_GUEST_SUBSCRIPTIONID=${AZURE_GUEST_SUBSCRIPTIONID}"
    - "AZURE_GUEST_TENANTID=${AZURE_GUEST_TENANTID}"
    - "AZURE_LOCATION=${AZURE_LOCATION}"
    - "AZURE_MASTER_SUBNET_CIDR=${AZURE_MASTER_SUBNET_CIDR}"
    - "AZURE_SUBSCRIPTIONID=${AZURE_SUBSCRIPTIONID}"
    - "AZURE_TENANTID=${AZURE_TENANTID}"
    - "AZURE_WORKER_SUBNET_CIDR=${AZURE_WORKER_SUBNET_CIDR}"
    - "CIRCLECI=${CIRCLECI}"
    - "CIRCLE_SHA1=${CIRCLE_SHA1}"
    - "CIRCLE_BUILD_NUM=${CIRCLE_BUILD_NUM}"
    - "CLUSTER_NAME=${CLUSTER_NAME}"
    - "COMMON_DOMAIN=${COMMON_DOMAIN}"
    - "COMMON_DOMAIN_RESOURCE_GROUP=${COMMON_DOMAIN_RESOURCE_GROUP}"
    - "GITHUB_BOT_TOKEN=${GITHUB_BOT_TOKEN}"
    - "IDRSA_PUB=${IDRSA_PUB}"
    - "IGNITION_DEBUG_ENABLED=${IGNITION_DEBUG_ENABLED}"
    - "IGNITION_DEBUG_LOGS_PREFIX=${IGNITION_DEBUG_LOGS_PREFIX}"
    - "IGNITION_DEBUG_LOGS_TOKEN=${IGNITION_DEBUG_LOGS_TOKEN}"
    - "KEEP_RESOURCES=${KEEP_RESOURCES}"
    - "REGISTRY_PULL_SECRET=${REGISTRY_PULL_SECRET}"
    - "TEST_DIR=${TEST_DIR}"
    - "TESTED_VERSION=${TESTED_VERSION}"
    - "VAULT_TOKEN=mytoken"